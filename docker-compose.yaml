version: '3.1'

services:

  php:
    image: php:latest
    volumes:
      - ./src:/var/www/html
    ports:
      - "8089:80"
    depends_on:
      - mysql
    links:
      - mysql
   
  mysql:
    image: mysql:5.6
    restart: always
    volumes:
      - ./mysql:/var/lib/mysql
      
    environment:
      MYSQL_ROOT_PASSWORD: password
    ports:
      - "3607:3306"
    command: --default-authentication-plugin=mysql_native_password --sql-mode="" 

  adminer:
    image: adminer
    restart: always
    ports:
      - 8099:8080
    depends_on:
      - mysql
    links:
      - mysql
